#include <stdio.h>
#include <stdlib.h> // for malloc

struct num {
        int val;
        struct num *next;
};

int main()
{
        struct num *start = NULL;
        struct num *last  = NULL;
        struct num *new   = NULL;

        return(0);
}
