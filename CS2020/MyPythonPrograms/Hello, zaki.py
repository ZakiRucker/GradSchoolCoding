#print "Hello, Zaki"
#if isAgeValid(age) and isHeightValid(height) :
    
#    recommendWgt = getRecommendWgt(age, height)
    
#    print("Recommended Wgt for %d-year-old %dcm person is %1.1fkg" % \
           (age, height, recommendWgt))
    
#else:
    
#    print("Program Aborted: Invalid Input")

#for i in range(1, 11):
#   format = "%"+ str(11-i) + "s"
#   print(format % "*")



def ZEQ(x, y) :     # Notice that x and y are local to this function.
    Z = x * y 
    return Z

x = input("Enter a positive integer:")

    if x = 0
        Z = str(undefined)

    if x = 1

    cnt = x
    while cnt > 1:
        y = x - 1
        Z = x * y
        x = x - 1

print Z
